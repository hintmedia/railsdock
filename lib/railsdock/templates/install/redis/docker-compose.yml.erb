

  redis:
    env_file:
      - ./.env.railsdock
      - ./.env
    build:
      context: <%= dockerfile_dir %>redis
      args:
        - REDIS_VERSION=${REDIS_VERSION}
    ports:
      - "${REDIS_HOST_PORT}:6379"
    volumes:
      - redis:/data